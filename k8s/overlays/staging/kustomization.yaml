resources:
  - ../../base

namePrefix: staging-
labels:
- includeSelectors: true
  pairs:
    environment: staging
patches:
  - target:
      kind: Deployment
      name: mock-app
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
